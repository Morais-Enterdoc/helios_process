name: helios_process
description: "Sistema interno de consultoria e gestão de processos"

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  file_picker: ^10.1.9
  file_selector: ^1.0.3
  csv: ^6.0.0
  excel: ^4.0.6
  pdf: ^3.11.1
  path_provider: ^2.1.5
  flutter_localizations:
    sdk: flutter
  sqflite_common_ffi: ^2.3.3
  path: ^1.9.0
  share_plus: ^10.0.2
  http: ^1.2.2
  url_launcher: ^6.3.1
  flutter_dotenv: ^5.1.0
 


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - .env
    - assets/imagens/EnterDoc.png
  
